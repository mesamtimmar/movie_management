<div class="row">
  <% movies.each do |movie| %>
    <div class='col-sm-3 col-lg-3 col-md-3'>
      <%= link_to movie do %>
        <div class='thumbnail hvr-float'>
          <p><%= image_tag movie.top_poster(:medium), class:'poster-image' %></p>

          <div class='caption movie_caption'>
            <%= movie.title %>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
</div>

<div class="row">
  <div class="col-sm-12">
    <%= link_to "View all", movies_path(filter: category), class: 'btn btn-default pull-right' %>
  </div>
</div>
