<%= form_for([@movie, @review], remote: true) do |f| %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class='row'>
    <div class='field col-md-8 col-md-offset-1'>
      <%= f.text_area :comment, required: true, placeholder: 'Your review', minlength: 5, maxlength: 500, class: 'form-control', id: 'review_text' %>
      <div id='review_error'></div>
    </div>
  </div>

  <div class='row'>
    <div class='field col-md-1 col-md-offset-8'>
      <div class='actions'>
        <%= f.submit 'Add', class: 'btn btn-primary'  %>
      </div>
    </div>
  </div>
<% end %>
